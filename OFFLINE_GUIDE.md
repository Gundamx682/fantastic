# 离线依赖包制作指南

## 📦 完整的离线安装解决方案

### 🎯 解决的问题
- **内存不足**: yum安装时进程被杀死
- **网络问题**: 在线依赖安装失败
- **环境限制**: 无法访问某些软件仓库

## 🛠️ 制作步骤

### 第一步：在完整的CentOS系统上下载依赖

```bash
# 1. 下载依赖下载脚本
curl -L -o download-deps.sh https://raw.githubusercontent.com/Gundamx682/fantastic/main/download-deps.sh
chmod +x download-deps.sh

# 2. 运行下载脚本
sudo ./download-deps.sh

# 3. 创建压缩包
cd offline-deps
./create-package.sh
```

### 第二步：上传到GitHub仓库

将生成的 `apk-downloader-deps.tar.gz` 文件上传到您的 `Gundamx682/fantastic` 仓库。

### 第三步：用户一键安装

用户现在可以使用以下命令一键安装：

```bash
curl -fsSL https://raw.githubusercontent.com/Gundamx682/fantastic/main/install.sh | sudo bash
```

## 📁 文件结构

```
fantastic/
├── install.sh                    # 主安装脚本（支持离线依赖）
├── apk-downloader.sh             # APK下载脚本
├── apk-server.py                 # HTTP服务器
├── apk-downloader.service        # systemd服务配置
├── apk-server.service            # HTTP服务配置
├── download-deps.sh              # 依赖下载脚本
├── offline-deps.tar.gz           # 离线依赖包（需要上传）
└── README.md                     # 项目文档
```

## 🔧 工作原理

### 安装流程
1. **检查离线依赖包** - 尝试下载 `offline-deps.tar.gz`
2. **离线安装** - 如果下载成功，解压并运行离线安装
3. **在线安装** - 如果离线失败，回退到在线安装
4. **继续部署** - 依赖安装完成后继续主程序部署

### 离线依赖包内容
```
offline-deps/
├── rpms/                         # RPM包文件
│   ├── curl-*.rpm
│   ├── wget-*.rpm
│   ├── jq-*.rpm
│   ├── python3-*.rpm
│   ├── python3-pip-*.rpm
│   ├── systemd-*.rpm
│   └── firewalld-*.rpm
├── python/                       # Python依赖
│   ├── requirements.txt
│   ├── setuptools-*.whl
│   ├── pip-*.whl
│   └── wheel-*.whl
├── offline-install.sh            # 离线安装脚本
└── README.md                     # 使用说明
```

## 🎯 优势

1. **无需网络** - 完全离线安装依赖
2. **内存友好** - 本地RPM安装，无需yum解析依赖
3. **快速部署** - 一键安装，无需手动干预
4. **容错性强** - 离线失败自动回退在线安装
5. **通用兼容** - 支持CentOS 7/8/9

## 📋 使用场景

- **内存受限的VPS** - 小内存云服务器
- **内网环境** - 无法访问外网的服务器
- **批量部署** - 多台服务器快速部署
- **网络不稳定** - 避免网络问题影响安装

## 🚀 快速开始

1. **制作依赖包**（在有网络的CentOS上）:
   ```bash
   curl -L -o download-deps.sh https://raw.githubusercontent.com/Gundamx682/fantastic/main/download-deps.sh
   chmod +x download-deps.sh
   sudo ./download-deps.sh
   ```

2. **上传依赖包**到GitHub仓库

3. **用户安装**（在目标服务器上）:
   ```bash
   curl -fsSL https://raw.githubusercontent.com/Gundamx682/fantastic/main/install.sh | sudo bash
   ```

## 💡 注意事项

- 依赖包需要在与目标系统相同版本的CentOS上制作
- 不同的系统架构需要不同的依赖包
- 定期更新依赖包以保持最新版本
- 测试离线包在目标环境中的兼容性